const today = new Date();
let myNumber: number;
myNumber = 5;

const person = {
 age: 20
};

class Color {
  red: number;
  green: number;
  blue: number;
}

const red = new Color();
red.red = 255;

interface RGB {
  red: number;
  green: number;
  blue: number;
}